import { useRouteMatch } from "react-router-dom";
import { useParams, useHistory, useLocation } from "react-router-dom"

export default function Movie() {
 
    
    return(
        <>
        <h1>Some movie with </h1>
        </>
    );
}; 